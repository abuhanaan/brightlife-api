openapi: "3.0.0"

info:
  version: 1.0.0
  title: Bright-life Enhancement API
  description:
    "API documentation for Bright-life Enhancement project"
  license:
    name: "fronteersdev Inc"
    url: "https://fronteersdev.com/license"

servers:
  - url: http://localhost:7081
    description: Local test server
  - url: https://bright-life-enhancement.fronteers-dev.com/
    description: Dev test server

security:
  - BearerAuth: [ ]  # ⬅️ Apply JWT security globally to all endpoints
components:
  securitySchemes:
    BearerAuth:
      type: http
      description: This API uses JWT for authentication
      scheme: bearer
      bearerFormat: JWT

  parameters:
    PageParam:
      explode: true
      in: query
      name: page
      required: false
      schema:
        type: integer
        minimum: 1
        default: 1
      style: form

    limitParam:
      explode: true
      in: query
      name: limit
      required: false
      schema:
        format: int32
        maximum: 100
        type: integer
      style: form

    PerPageParam:
      in: query
      name: perPage
      schema:
        type: integer
        default: 10
        description: The number of items to return per page.
        minimum: 1
    idParam:
      in: path
      name: idParam
      description: A unique id to update or fetch a record.
      required: true
      schema:
        type: integer
        format: int64
    patientIdParam:
      in: path
      name: patientId
      description: A unique id to update or fetch a patients record.
      required: true
      schema:
        type: string

  schemas:
    ADHDForm:
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        projectCompletionProblem:
          $ref: '#/components/schemas/AssessmentEnum'
        organizationRate:
          $ref: '#/components/schemas/AssessmentEnum'
        memoryRate:
          $ref: '#/components/schemas/AssessmentEnum'
        attitudeToChallenge:
          $ref: '#/components/schemas/AssessmentEnum'
        fidgetRateOnsit:
          $ref: '#/components/schemas/AssessmentEnum'
        activeToWork:
          $ref: '#/components/schemas/AssessmentEnum'
        carelessMistakes:
          $ref: '#/components/schemas/AssessmentEnum'
        attentionToBoringWork:
          $ref: '#/components/schemas/AssessmentEnum'
        concentrationRate:
          $ref: '#/components/schemas/AssessmentEnum'
        misplaceRate:
          $ref: '#/components/schemas/AssessmentEnum'
        distractionRate:
          $ref: '#/components/schemas/AssessmentEnum'
        excuseRate:
          $ref: '#/components/schemas/AssessmentEnum'
        restlessRate:
          $ref: '#/components/schemas/AssessmentEnum'
        troubleRelaxing:
          $ref: '#/components/schemas/AssessmentEnum'
        excessiveTalks:
          $ref: '#/components/schemas/AssessmentEnum'
        peopleSentenceCompletion:
          $ref: '#/components/schemas/AssessmentEnum'
        patienceOnQueue:
          $ref: '#/components/schemas/AssessmentEnum'
        interruptOthers:
          $ref: '#/components/schemas/AssessmentEnum'
      type: object

    AssessmentEnum:
      type: string
      enum:
        - Never
        - Rarely
        - Sometimes
        - Often
        - Very Often

    AnxietyDisorderForm:
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        nervousRate:
          type: string
        controlOverWorry:
          type: string
        excessiveWorry:
          type: string
        relaxTrouble:
          type: string
        restlessness:
          type: string
        annoyanceRate:
          type: string
        frightRate:
          type: string
        lifeInfluenceSummary:
          type: string
      type: object

    ControlledSubstanceForm:
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        isMinor:
          type: boolean
        patientSignDate:
          $ref: "#/components/schemas/DateTime"
        guardianName:
          type: string
        patientGuardianRelationship:
          type: string
        guardianSignDate:
          $ref: "#/components/schemas/DateTime"
        file:
          $ref: '#/components/schemas/FileUpload'
      type: object

    DepressionAssessmentForm:
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        pleasureInterest:
          $ref: '#/components/schemas/AssessmentLimit'
        depressionRate:
          $ref: '#/components/schemas/AssessmentLimit'
        sleepRate:
          $ref: '#/components/schemas/AssessmentLimit'
        fatigueRate:
          $ref: '#/components/schemas/AssessmentLimit'
        appetiteRate:
          $ref: '#/components/schemas/AssessmentLimit'
        failureRate:
          $ref: '#/components/schemas/AssessmentLimit'
        concentrationRate:
          $ref: '#/components/schemas/AssessmentLimit'
        restlessnessRate:
          $ref: '#/components/schemas/AssessmentLimit'
        suicideThought:
          $ref: '#/components/schemas/AssessmentLimit'
      type: object

    AssessmentLimit:
      type: integer
      minimum: 0
      maximum: 3

    PatientInformationConsentAndFinancialPolicyForm:
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        date:
          $ref: "#/components/schemas/Date"
        patientInformationConsentAndFinancialPolicy:
          type: string
        file:
          $ref: '#/components/schemas/FileUpload'
      type: object

    Address:
      properties:
        id:
          type: integer
          format: int64
        streetName:
          type: string
        city:
          type: string
        state:
          type: string
        zipCode:
          type: string
      type: object

    Pharmacy:
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        name:
          type: string
        phone:
          type: string
        address:
          $ref: '#/components/schemas/Address'
      type: object

    PrimaryCarePhysician:
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        havePcp:
          type: string
        name:
          type: string
        phone:
          type: string
        fax:
          type: string
        address:
          $ref: '#/components/schemas/Address'
      type: object

    InitialEvaluationForm:
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        pharmacy:
          $ref: '#/components/schemas/Pharmacy'
        primaryCarePhysician:
          $ref: '#/components/schemas/PrimaryCarePhysician'
        infoToRelease:
          type: string
        date:
          $ref: "#/components/schemas/Date"
        initialEvaluation:
          type: string
        file:
          $ref: '#/components/schemas/FileUpload'
      type: object

    IntakeForm:
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        doYouShareHome:
          type: string
        complaints:
          type: string
        sexPreference:
          type: string
        onProbation:
          type: string
        inLawsuit:
          type: string
        childrenCount:
          type: integer
        marriageCount:
          type: integer
        pastMarriagesInfo:
          items:
            $ref: '#/components/schemas/PastMarriagesInfo'
          type: array
        pastProviders:
          items:
            $ref: '#/components/schemas/PastProviders'
          type: array
        currentMedications:
          items:
            $ref: '#/components/schemas/Medication'
          type: array
        hasAttemptedSuicide:
          type: boolean
        isPsychHospitalized:
          type: boolean
        pastMedications:
          type: array
          items:
            $ref: '#/components/schemas/Medication'
        alcoholDrugHistory:
          $ref: '#/components/schemas/AlcoholDrugHistory'
      type: object


    FrequencyEnum:
      type: string
      enum:
        - None
        - Rarely
        - Sometimes
        - Often

    DrinkGuiltCheck:
      type: object
      properties:
        haveCutBack:
          type: boolean
        angeredByCritics:
          type: boolean
        feelGuilt:
          type: boolean
        upWithDrink:
          type: boolean

    AlcoholDrugHistory:
      properties:
        id:
          type: integer
          format: int64
        usageFrequency:
          $ref: '#/components/schemas/FrequencyEnum'
        brand:
          type: string
        lastUsed:
          type: string
        drinkGuiltCheck:
          $ref: '#/components/schemas/DrinkGuiltCheck'
        substanceUsages:
          type: array
          items:
            $ref: '#/components/schemas/SubstanceUsage'
        weeklyAverageSpending:
          type: double
        pastTreatmentInfo:
          type: array
          items:
            $ref: '#/components/schemas/PastTreatmentInfo'
        isPastStepRecoveryParticipant:
          type: boolean
        isCurrentStepRecoveryParticipant:
          type: boolean
        birthPlace:
          type: string
        growthPlace:
          type: string
        raisedBy:
          type: string
        siblingsCount:
          type: integer
        childhoodInfo:
          type: string
        wasPhysicallyAbused:
          type: boolean
        wasEmotionallyAbused:
          type: boolean
        wasSexuallyAbused:
          type: boolean
        hasMedicalDisability:
          type: boolean
        pastMedicalHistory:
          type: array
          items:
            type: string
        pastSurgicalHistory:
          type: array
          items:
            type: string
        allergies:
          type: array
          items:
            type: string
        relativesWithMentalIllnessOrSuicide:
          type: array
          items:
            $ref: '#/components/schemas/RelativeWithMentalIllnessOrSuicide'
        otherUsefulInfo:
          type: string
      type: object

    RelativeWithMentalIllnessOrSuicide:
      properties:
        id:
          type: integer
          format: int64
        relative:
          type: string
        illness:
          type: string
      type: object

    PastTreatmentInfo:
      properties:
        id:
          type: integer
          format: int64
        facility:
          type: string
        date:
          type: string
          format: date
        drugTreated:
          type: string
        isTreatmentCompleted:
          type: boolean
      type: object

    SubstanceUsage:
      properties:
        id:
          type: integer
          format: int64
        substanceName:
          type: string
          enum:
            - Alcohol
            - Marijuana
            - Cocaine
            - Crystal Meth
            - Ice
            - Adderall
            - Vyvanse
            - Ritaline
            - LSD
            - XTC/Molly
            - Peyote
            - Ketamine
            - Mushroom
            - Xanax
            - Valium
            - Klonapin
            - Activan
            - Dextromethorphan
            - Steroids
            - Spice
            - Bath Salts
        ageAtFirstUse:
          type: integer
        qtyUse:
          type: string
        usageFrequency:
          $ref: '#/components/schemas/FrequencyEnum'
        lastUsed:
          type: string
      type: object

    PastProviders:
      properties:
        id:
          type: integer
          format: int64
        provider:
          type: string
        appointmentDate:
          type: string
          format: date
      type: object

    Medication:
      properties:
        id:
          type: integer
          format: int64
        medication:
          type: string
        usageInstruction:
          type: string
        conditionTreated:
          type: string
        prescription:
          type: string
        category:
          type: string
          enum:
            - past
            - current
      type: object

    PastMarriagesInfo:
      properties:
        id:
          type: integer
          format: int64
        marriageDescription:
          type: string
        duration:
          type: string
        divorceReason:
          type: string
      type: object

    MedicationConsentForm:
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        patientSignDate:
          $ref: "#/components/schemas/Date"
        isMinor:
          type: boolean
        guardianName:
          type: string
        patientGuardianRelationship:
          type: string
        guardianSignDate:
          $ref: "#/components/schemas/Date"
        medicationConsent:
          type: string
        file:
          $ref: '#/components/schemas/FileUpload'
      type: object

    MoodDisorderAssessmentForm:
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        hyperFeeling:
          type: boolean
        isIrritable:
          type: boolean
        isOverConfident:
          type: boolean
        lessSleep:
          type: boolean
        talkMore:
          type: boolean
        pacedThoughts:
          type: boolean
        easyDistraction:
          type: boolean
        overEnergetic:
          type: boolean
        overActive:
          type: boolean
        overSocial:
          type: boolean
        sexaholic:
          type: boolean
        overFoolish:
          type: boolean
        overSpending:
          type: boolean
        sameTimeOccurrence:
          type: boolean
        influenceOnLife:
          type: boolean
        isRelativeWithBipolar:
          type: boolean
        isBipolarDiagnosed:
          type: boolean
      type: object

    GenderEnum:
      type: string
      enum:
        - Male
        - Female
        - Transgender
        - Declined
        - Non-Binary
        - Prefer Not To Say

    MaritalStatusEnum:
      type: string
      enum:
        - Single
        - Married
        - Divorced
        - Widowed
        - Separated
        - Domestic Partnership
        - Prefer Not To Say

    EducationLevelEnum:
      type: string
      enum:
        - High School Diploma
        - Associate Degree
        - Bachelor's Degree
        - Master's Degree
        - Professional Degree
        - Doctoral Degree

    ReligionEnum:
      type: string
      enum:
        - Islam
        - Christianity
        - Judaism
        - Hinduism
        - Buddhism
        - Sikhism
        - Atheism
        - Agnosticism
        - Other
        - Prefer Not To Say

    RaceEnum:
      type: string
      enum:
        - White
        - Black Or African American
        - Asian
        - Native American Or Alaska Native
        - Multiracial
        - Prefer Not To Say

    EmploymentStatusEnum:
      type: string
      enum:
        - Employed
        - Self-Employed
        - Unemployed
        - Retired
        - Student
        - Homemaker
        - Other

    PersonalInfo:
      type: object
      properties:
        firstName:
          type: string
        middleName:
          type: string
        lastName:
          type: string
        gender:
          $ref: "#/components/schemas/GenderEnum"
        dob:
          $ref: "#/components/schemas/Date"
        maritalStatus:
          $ref: "#/components/schemas/MaritalStatusEnum"
        socialSecurityNumber:
          type: string
        homePhone:
          type: string
        cellPhone:
          type: string
        workPhone:
          type: string
        preferredPhone:
          type: string
          enum:
            - Home Phone
            - Cell Phone
            - Work Phone
        appointmentReminderMode:
          type: string
          enum:
            - Automated Text
            - Automated Email
        email:
          $ref: "#/components/schemas/Email"
        sendMsgToHomePhone:
          type: boolean
        sendMsgToRelative:
          type: boolean
        sendMsgToWork:
          type: boolean
        sendMsgToCellPhone:
          type: boolean
        address:
          $ref: '#/components/schemas/Address'
        highestEduLevel:
          $ref: "#/components/schemas/EducationLevelEnum"
        employmentStatus:
          $ref: "#/components/schemas/EmploymentStatusEnum"
        employer:
          type: string
        occupation:
          type: string
        religion:
          $ref: "#/components/schemas/ReligionEnum"
        ethnicity:
          $ref: "#/components/schemas/EthnicityEnum"
        race:
          $ref: "#/components/schemas/RaceEnum"
        preferredLanguage:
          type: string

    EthnicityEnum:
      type: string
      enum:
        - Hispanic or Latino
        - Not Hispanic or Latino
        - Prefer Not To Say

    Guarantor:
      type: object
      properties:
        id:
          type: integer
          format: int64
        firstName:
          type: string
        lastName:
          type: string
        dob:
          $ref: "#/components/schemas/Date"
        relationship:
          type: string
        address:
          $ref: '#/components/schemas/Address'
        phone:
          type: string
        email:
          $ref: "#/components/schemas/Email"
        stateIssuedId:
          type: string
        insuranceCard:
          type: string
        stateIssuedIdFile:
          $ref: '#/components/schemas/FileUpload'
        insuranceCardFile:
          $ref: '#/components/schemas/FileUpload'

    FileUpload:
      type: string
      format: binary

    ParentGuardian:
      type: object
      properties:
        id:
          type: integer
          format: int64
        firstName:
          type: string
        lastName:
          type: string
        gender:
          $ref: '#/components/schemas/GenderEnum'
        maritalStatus:
          $ref: '#/components/schemas/MaritalStatusEnum'
        phone:
          type: string
        email:
          $ref: "#/components/schemas/Email"
        familyRole:
          type: string
          enum:
            - Father
            - Mother
            - Husband
            - Son
            - Daughter
            - Other
        employmentStatus:
          $ref: '#/components/schemas/EmploymentStatusEnum'
        employer:
          type: string
        occupation:
          type: string
        address:
          $ref: '#/components/schemas/Address'

    EmergencyContact:
      type: object
      properties:
        id:
          type: integer
          format: int64
        firstName:
          type: string
        lastName:
          type: string
        relationship:
          type: string
        address:
          $ref: '#/components/schemas/Address'
        homePhone:
          type: string
        cellPhone:
          type: string
        email:
          $ref: "#/components/schemas/Email"

    PaymentStructure:
      type: object
      properties:
        paymentMode:
          $ref: '#/components/schemas/PaymentModeEnum'
        insurances:
          type: array
          items:
            $ref: '#/components/schemas/Insurance'
          maxItems: 2

    PaymentModeEnum:
      type: string
      enum:
        - Self Pay
        - Insurance Card

    PolicyHolder:
      type: object
      properties:
        firstName:
          type: string
        middleName:
          type: string
        lastName:
          type: string
        relationship:
          type: string
        phone:
          type: string
        dob:
          $ref: "#/components/schemas/Date"

    InsuranceProvider:
      type: object
      properties:
        name:
          $ref: '#/components/schemas/InsuranceNameEnum'
        phone:
          type: string
        policyId:
          type: string
        groupNumber:
          type: string
        authorizationId:
          type: string
        coPay:
          type: string
        coverageStartDate:
          $ref: "#/components/schemas/Date"
        coverageEndDate:
          $ref: "#/components/schemas/Date"
        address:
          $ref: '#/components/schemas/Address'

    Insurance:
      type: object
      properties:
        id:
          type: integer
          format: int64
        primary:
          type: boolean
        policyHolder:
          $ref: '#/components/schemas/PolicyHolder'
        insuranceProvider:
          $ref: '#/components/schemas/InsuranceProvider'

    PatientRegistrationForm:
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        personalInfo:
          $ref: '#/components/schemas/PersonalInfo'
        guarantor:
          $ref: '#/components/schemas/Guarantor'
        parentGuardian:
          $ref: '#/components/schemas/ParentGuardian'
        emergency:
          $ref: '#/components/schemas/EmergencyContact'
        paymentStructure:
          $ref: '#/components/schemas/PaymentStructure'
        date:
          $ref: "#/components/schemas/Date"
        patientRegForm:
          type: string
        file:
          $ref: '#/components/schemas/FileUpload'
      type: object

    NoticeOfPrivacyPracticesForm:
      type: object
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        noticeEffectDate:
          $ref: "#/components/schemas/Date"
        date:
          $ref: "#/components/schemas/Date"
        noticeOfPrivacyPractices:
          type: string
        file:
          $ref: '#/components/schemas/FileUpload'

    Party:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        phoneNumber:
          type: string
        fax:
          type: string
        isReceive:
          type: boolean
        address:
          $ref: '#/components/schemas/Address'

    ReleaseReceiveForm:
      type: object
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        isMinor:
          type: boolean
        releaseHealthInfo:
          type: boolean
        receiveHealthInfo:
          type: boolean
        exchangeHealthInfo:
          type: boolean
        party:
          type: array
          items:
            $ref: '#/components/schemas/Party'
        disclosurePurpose:
          type: array
          items:
            type: string
            enum:
              - Continuity of Care/Treatment
              - Legal Purposes
              - Insurance/Payment Processing
              - Personal Use
              - Other
        infoTypeToRelease:
          type: array
          items:
            type: string
            enum:
              - Mental Health Treatment Records
              - Substance Use Disorder Treatment Records
              - Psychotherapy Notes
              - Assessment/Evaluation Reports
              - Medication History & Prescriptions
              - Billing & Insurance Records
              - Discharge Summary
              - Other
        guardianName:
          type: string
        relationship:
          type: string
        date:
          $ref: "#/components/schemas/Date"
        releaseReceive:
          type: string
        file:
          $ref: '#/components/schemas/FileUpload'

    ScreeningForm:
      type: object
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        mhBhPhone:
          type: string
        helpNeeds:
          type: string
        inCrisis:
          type: boolean
        currentlyOnPsychMed:
          type: boolean
        stableOnMed:
          type: boolean
        isPsychiatristConsult:
          type: boolean
        isTherapistConsult:
          type: boolean
        anyMentalHealthTreatment:
          type: boolean
        suicideAttemptHistory:
          type: boolean
        harmToSelfOrOthers:
          type: boolean
        intent:
          type: string
        healthSymptoms:
          type: boolean
        healthSymptomsFrequency:
          type: string
        referral:
          $ref: '#/components/schemas/Referral'

    Referral:
      type: object
      properties:
        id:
          type: integer
          format: int64
        source:
          type: string
          enum:
            - Insurance
            - Internet
            - Friend
            - Other
        therapist:
          type: string
          enum:
            - Mental Health Therapist/MD
            - Facility
        firstName:
          type: string
        middleName:
          type: string
        lastName:
          type: string
        phone:
          type: string
        address:
          $ref: '#/components/schemas/Address'

    SelfPayForm:
      type: object
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        date:
          $ref: "#/components/schemas/Date"
        selfPay:
          type: string
        file:
          $ref: '#/components/schemas/FileUpload'

    TerminationPolicyForm:
      type: object
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        patientSignDate:
          $ref: "#/components/schemas/Date"
        witnessName:
          type: string
        witnessSignDate:
          $ref: "#/components/schemas/Date"
        terminationPolicy:
          type: string
        file:
          $ref: '#/components/schemas/FileUpload'

    TreatmentConsentTelehealthInPersonTreatmentConsent:
      type: object
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        patientSignDate:
          $ref: "#/components/schemas/Date"
        isMinor:
          type: boolean
        guardianName:
          type: string
        guardianSignDate:
          $ref: "#/components/schemas/Date"
        treatmentConsentTelehealthInPerson:
          type: string
        file:
          $ref: '#/components/schemas/FileUpload'

    PaginatedReviews:
      type: object
      properties:
        currentPage:
          type: integer
          example: 1
        totalPages:
          type: integer
          example: 10
        itemsPerPage:
          type: integer
          example: 100
        reviews:
          type: array
          items:
            $ref: "#/components/schemas/Review"

    Review:
      type: object
      properties:
        id:
          type: integer
          format: int64
        nickname:
          type: string
        email:
          type: string
        referralWish:
          type: string
        rating:
          type: integer
          minimum: 1
          maximum: 5
        reviewMessage:
          type: string
        status:
          $ref: '#/components/schemas/ReviewStatusEnum'

    ReviewStatusEnum:
      type: string
      enum:
        - draft
        - published

    Forms:
      type: object
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        adhdForm:
          $ref: '#/components/schemas/ADHDForm'
        anxietyDisorderForm:
          $ref: '#/components/schemas/AnxietyDisorderForm'
        controlledSubstanceForm:
          $ref: '#/components/schemas/ControlledSubstanceForm'
        depressionAssessmentForm:
          $ref: '#/components/schemas/DepressionAssessmentForm'
        patientInformationConsentAndFinancialPolicyForm:
          $ref: '#/components/schemas/PatientInformationConsentAndFinancialPolicyForm'
        initialEvaluationForm:
          $ref: '#/components/schemas/InitialEvaluationForm'
        intakeForm:
          $ref: '#/components/schemas/IntakeForm'
        relativeWithMentalIllnessOrSuicide:
          $ref: '#/components/schemas/RelativeWithMentalIllnessOrSuicide'
        medicationConsentForm:
          $ref: '#/components/schemas/MedicationConsentForm'
        moodDisorderAssessmentForm:
          $ref: '#/components/schemas/MoodDisorderAssessmentForm'
        patientRegistrationForm:
          $ref: '#/components/schemas/PatientRegistrationForm'
        noticeOfPrivacyPracticesForm:
          $ref: '#/components/schemas/NoticeOfPrivacyPracticesForm'
        releaseReceiveForm:
          $ref: '#/components/schemas/ReleaseReceiveForm'
        screeningForm:
          $ref: '#/components/schemas/ScreeningForm'
        selfPayForm:
          $ref: '#/components/schemas/SelfPayForm'
        treatmentConsentTelehealthInPersonTreatmentConsent:
          $ref: '#/components/schemas/TreatmentConsentTelehealthInPersonTreatmentConsent'
        terminationPolicy:
          $ref: '#/components/schemas/TerminationPolicyForm'

    Patient:
      type: object
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        forms:
          $ref: '#/components/schemas/Forms'
        reviews:
          type: array
          items:
            $ref: '#/components/schemas/Review'
            maxItems: 100
        appointments:
          type: array
          items:
            $ref: '#/components/schemas/Appointment'
            maxItems: 100

    PaginatedPatients:
      type: object
      properties:
        currentPage:
          type: integer
          example: 1
        totalPages:
          type: integer
          example: 10
        itemsPerPage:
          type: integer
          example: 100
        patients:
          type: array
          items:
            $ref: "#/components/schemas/Patient"

    PaginatedAppointments:
      type: object
      properties:
        currentPage:
          type: integer
          example: 1
        totalPages:
          type: integer
          example: 10
        itemsPerPage:
          type: integer
          example: 100
        appointments:
          type: array
          items:
            $ref: "#/components/schemas/Appointment"

    Appointment:
      type: object
      properties:
        id:
          type: integer
          format: int64
        patientId:
          $ref: "#/components/schemas/PatientId"
        isNew:
          type: boolean
        verificationStatus:
          type: string
        firstName:
          type: string
        middleName:
          type: string
        lastName:
          type: string
        gender:
          $ref: '#/components/schemas/GenderEnum'
        dob:
          $ref: "#/components/schemas/Date"
        phone:
          type: string
        email:
          $ref: "#/components/schemas/Email"
        address:
          $ref: '#/components/schemas/Address'
        appointmentType:
          type: string
          enum:
            - In-Person
            - Tele-Health
            - Any
        service:
          type: string
          enum:
            - Med Management
            - Individual Therapy
            - Group Therapy
            - Case Management
            - Psychiatric Rehab.
        appointmentDateTime:
          $ref: "#/components/schemas/DateTime"
        purpose:
          type: string
        paymentMethod:
          $ref: '#/components/schemas/PaymentModeEnum'
        insuranceName:
          $ref: '#/components/schemas/InsuranceNameEnum'
        insuranceNumber:
          type: string

    InsuranceNameEnum:
      type: string
      enum:
        - Medicare
        - Main Medicaid
        - BCBS Carefirst
        - UHC/Optum/Medicaid
        - Maryland Physicians Care
        - Amerigroup

    PatientSearch:
      type: object
      properties:
        patientId:
          $ref: "#/components/schemas/PatientId"
        firstName:
          type: string
        lastName:
          type: string
        middleName:
          type: string
        dob:
          $ref: "#/components/schemas/Date"
        phone:
          type: string
        email:
          $ref: "#/components/schemas/Email"
        gender:
          $ref: "#/components/schemas/GenderEnum"
        maritalStatus:
          $ref: "#/components/schemas/MaritalStatusEnum"
        city:
          type: string
        state:
          type: string
        paymentMode:
          $ref: '#/components/schemas/PaymentModeEnum'

    AppointmentSearch:
      type: object
      properties:
        id:
          type: integer
          format: int64
        firstName:
          type: string
        lastName:
          type: string
        middleName:
          type: string
        dob:
          $ref: "#/components/schemas/Date"
        phone:
          type: string
        email:
          $ref: "#/components/schemas/Email"
        gender:
          $ref: "#/components/schemas/GenderEnum"
        maritalStatus:
          $ref: "#/components/schemas/MaritalStatusEnum"
        city:
          type: string
        state:
          type: string
        appointmentDateTime:
          $ref: "#/components/schemas/DateTime"
        paymentMode:
          $ref: '#/components/schemas/PaymentModeEnum'

    ReviewSearch:
      type: object
      properties:
        name:
          type: string
        email:
          $ref: "#/components/schemas/Email"
        rating:
          type: integer
          minimum: 1
          maximum: 5
        status:
          $ref: '#/components/schemas/ReviewStatusEnum'

    JwtAuthenticationResponse:
      type: object
      properties:
        token:
          type: string

    SignInRequest:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          format: email
        password:
          type: string

    SignUpRequest:
      type: object
      required:
        - email
        - password
        - firstName
        - lastName
      properties:
        firstName:
          type: string
        lastName:
          type: string
        email:
          type: string
          format: email
        password:
          type: string

    PatientId:
      type: string
      format: uuid
    Email:
      type: string
      #      pattern: "^[A-Za-z0-9+_.-]+@[A-Za-z0-9.-]+${5,200}"
      format: email
    DateTime:
      type: string
      format: timestamp
    Date:
      type: string
      format: date

    CountryCode:
      type: string
      pattern: "^[A-Z]{2}$"

    CurrencyCode:
      type: string
      pattern: "^[A-Z]{3}$"

    Pagination:
      type: object
      properties:
        currentPage:
          type: integer
          minimum: 1
        totalPages:
          type: integer
          minimum: 0
        totalItems:
          type: integer
          minimum: 0
        itemsPerPage:
          type: integer
          minimum: 0

    Errors:
      type: array
      items:
        $ref: "#/components/schemas/Error"

    Error:
      description: |

        **The below table describes custom error codes returned by various APIs currently**
        | Code      | error         | Message                     |
        | ----------| --------------|-----------------------------|
        | 400       | INPUT         | BAD REQUEST                 |
        | 404       | NOT_FOUND     | RESOURCE DOES NOT EXIST     |
        | 422       | UNPROCESSABLE | UNPROCESSABLE ENTITY        |
        | 401       | UNAUTHORIZED  | UNAUTHORIZED                |
        | 403       | FORBIDDEN     | FORBIDDEN                   |
        | 500       | PROCESSING    | INTERNAL SERVER ERROR       |
        | 500       | TIMEOUT       | TIMEOUT                     |
      type: object
      required:
        - status
        - error
        - message
      properties:
        status:
          type: boolean
          default: false
        error:
          type: string
        message:
          type: string

    Success:
      type: object
      required:
        - status
        - success
        - message
      properties:
        status:
          type: boolean
          default: false
        success:
          type: string
        message:
          type: string

  responses:

    BadRequest:
#      type: object
      description: |
        Bad Request
        The request body or parameters does not conform to the schema.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

    Unauthorized:
      description: |
        Unauthorized.

        - Indicates the request does not contain a valid token to authenticate.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

    Forbidden:
      description: |
        Forbidden.
            - Indicates the authenticated entity does not have the privilege to perform
              the operation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

    UnProcessableEntity:
      description: |
        Unprocessable entity.
        - The request body may be well-formed, but either request payload or parameters failed semantic validation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

    InternalServerError:
      description: |
        Internal Server Error.
            - The server encountered an unexpected error while processing the request..
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

    ResourceNotFound:
      description: |
        Resource Not Found.
            - The requested resource not found.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

paths:
  /api/v1/patients/search:
    post:
      summary:
        Searches and returns all Patients matching all of the search criteria fields passed.
        The number search results shall be constrained by the limit params in Query.
        Search criteria, applies an **AND** based search on all fields passed in the request.
        If the search criteria is empty/null, it returns list of patient
      tags:
        - Patients
      operationId: listPatients
      parameters:
        - description: The page number (starting from 1)
          $ref: "#/components/parameters/PageParam"
        - description: How many items to return at one time (max 100)
          $ref: "#/components/parameters/limitParam"
      requestBody:
        description:
          "Search criteria, applies an **AND** based search on all fields\
                \ passed in the request."
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatientSearch"
      responses:
        "200":
          description: Patients Records Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedPatients"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/{patientId}:
    get:
      summary: Fetches A Patient's Details
      tags:
        - Patients
      operationId: getPatient
      parameters:
        - $ref: "#/components/parameters/patientIdParam"
      responses:
        "200":
          description: Patient Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Patient"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/register:
    post:
      summary: Registers A New Patient
      tags:
        - Patients
      operationId: register
      requestBody:
        description: This is used to register a new patient. A successful call returns a success response with a patient Id
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatientRegistrationForm"
      responses:
        "201":
          description: Patient Registered Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/register/update/{idParam}:
    put:
      summary: Updates A Patients Registration Form
      tags:
        - Patients
      operationId: updateRegistrationForm
      parameters:
        - $ref: "#/components/parameters/idParam"
      requestBody:
        description: This is used to update a patients registration form.
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatientRegistrationForm"
      responses:
        "201":
          description: Patient Registration Form Updated Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/register/{idParam}:
    get:
      summary: Fetches A Patients Registration Details
      tags:
        - Patients
      operationId: getRegistration
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient Registration Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PatientRegistrationForm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/adhd:
    post:
      summary: Submits A New ADHD Form For A Patient
      tags:
        - Patients
      operationId: submitAdhd
      requestBody:
        description: This is used to submit a new ADHD form an already existing patient
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ADHDForm"
      responses:
        "201":
          description: Patient ADHD Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/adhd/{idParam}:
    get:
      summary: Fetches A Patients ADHD Form Details
      tags:
        - Patients
      operationId: getAdhd
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient ADHD Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ADHDForm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/anxiety-disorder:
    post:
      summary: Submits A New Anxiety Disorder Form For A Patient
      tags:
        - Patients
      operationId: submitAnxietyDisorder
      requestBody:
        description: This is used to submit a new Anxiety Disorder form for an already existing patient
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnxietyDisorderForm"
      responses:
        "201":
          description: Patient Anxiety Disorder Form Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/anxiety-disorder/{idParam}:
    get:
      summary: Fetches A Patients Anxiety Disorder Form Details
      tags:
        - Patients
      operationId: getAnxietyDisorder
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient Anxiety Disorder Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnxietyDisorderForm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/controlled-substance:
    post:
      summary: Submits A New Controlled Substance Form For A Patient
      tags:
        - Patients
      operationId: submitControlledSubstance
      requestBody:
        description: This is used to submit a new Controlled Substance form for an already existing patient
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ControlledSubstanceForm"
      responses:
        "201":
          description: Patient Controlled Substance Form Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/controlled-substance/{idParam}:
    get:
      summary: Fetches A Patients Controlled Substance Form Details
      tags:
        - Patients
      operationId: getControlledSubstance
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient Controlled Substance Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlledSubstanceForm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/depression-assessment:
    post:
      summary: Submits A New DepressionAssessment Form For A Patient
      tags:
        - Patients
      operationId: submitDepressionAssessment
      requestBody:
        description: This is used to submit a new Depression Assessment Form for an already existing patient
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DepressionAssessmentForm"
      responses:
        "201":
          description: Patient Depression Assessment Form Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/depression-assessment/{idParam}:
    get:
      summary: Fetches A Patient's Depression Assessment Form Details
      tags:
        - Patients
      operationId: getDepressionAssessment
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient Controlled Substance Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DepressionAssessmentForm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/initial-evaluation:
    post:
      summary: Submits A New Initial Evaluation Form For A Patient
      tags:
        - Patients
      operationId: submitInitialEvaluation
      requestBody:
        description: This is used to submit a new Initial Evaluation Form for an already existing patient
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InitialEvaluationForm"
      responses:
        "201":
          description: Patient Initial Evaluation Form Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/initial-evaluation/{idParam}:
    get:
      summary: Fetches A Patient's Initial Evaluation Form Details
      tags:
        - Patients
      operationId: getInitialEvaluation
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient Initial Evaluation Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InitialEvaluationForm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/intake:
    post:
      summary: Submits A New Intake Form For A Patient
      tags:
        - Patients
      operationId: submitIntake
      requestBody:
        description: This is used to submit a new Intake Form for an already existing patient
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntakeForm"
      responses:
        "201":
          description: Patient Intake Form Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/intake/{idParam}:
    get:
      summary: Fetches A Patient's Intake Form Details
      tags:
        - Patients
      operationId: getIntake
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient Intake Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntakeForm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/medication-consent:
    post:
      summary: Submits A New Medication Consent Form For A Patient
      tags:
        - Patients
      operationId: submitMedicationConsent
      requestBody:
        description: This is used to submit a new Medication Consent Form for an already existing patient
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MedicationConsentForm"
      responses:
        "201":
          description: Patient Medication Consent Form Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/medication-consent/{idParam}:
    get:
      summary: Fetches A Patient's Medication Consent Form Details
      tags:
        - Patients
      operationId: getMedicationConsent
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient Medication Consent Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MedicationConsentForm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/mood-disorder-assessment:
    post:
      summary: Submits A New Mood Disorder Assessment Form For A Patient
      tags:
        - Patients
      operationId: submitMoodDisorderAssessment
      requestBody:
        description: This is used to submit a new MoodDisorderAssessment Form for an already existing patient
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MoodDisorderAssessmentForm"
      responses:
        "201":
          description: Patient Mood Disorder Assessment Form Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/mood-disorder-assessment/{idParam}:
    get:
      summary: Fetches A Patient's Mood Disorder Assessment Form Details
      tags:
        - Patients
      operationId: getMoodDisorderAssessment
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient Mood Disorder Assessment Form Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MoodDisorderAssessmentForm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/privacy-practices:
    post:
      summary: Submits A New Notice Of Privacy Practices Form For A Patient
      tags:
        - Patients
      operationId: submitNoticeOfPrivacyPractices
      requestBody:
        description: This is used to submit a new NoticeOfPrivacyPractices Form for an already existing patient
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NoticeOfPrivacyPracticesForm"
      responses:
        "201":
          description: Patient NoticeOfPrivacyPractices Form Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/privacy-practices/{idParam}:
    get:
      summary: Fetches A Patient's Notice Of Privacy Practices Assessment Form Details
      tags:
        - Patients
      operationId: getNoticeOfPrivacyPractices
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient NoticeOfPrivacyPractices Form Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoticeOfPrivacyPracticesForm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/info-and-fin-policy:
    post:
      summary: Submits A Patient Information Consent And Financial Policy Form For A Patient
      tags:
        - Patients
      operationId: submitPatientInformationConsentAndFinancialPolicy
      requestBody:
        description: This is used to submit a new PatientInformationConsentAndFinancialPolicy Form for an already existing patient
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatientInformationConsentAndFinancialPolicyForm"
      responses:
        "201":
          description: Patient PatientInformationConsentAndFinancialPolicy Form Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/info-and-fin-policy/{idParam}:
    get:
      summary: Fetches A Patient's Information Consent And Financial Policy Form Details
      tags:
        - Patients
      operationId: getPatientInformationConsentAndFinancialPolicy
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient PatientInformationConsentAndFinancialPolicy Form Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PatientInformationConsentAndFinancialPolicyForm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/release-receive:
    post:
      summary: Submits A Release Receive Form For A Patient
      tags:
        - Patients
      operationId: submitReleaseReceive
      requestBody:
        description: This is used to submit a new ReleaseReceive Form for an already existing patient
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReleaseReceiveForm"
      responses:
        "201":
          description: Patient ReleaseReceive Form Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/release-receive/{idParam}:
    get:
      summary: Fetches A Patient's ReleaseReceive Form Details
      tags:
        - Patients
      operationId: getReleaseReceive
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient ReleaseReceive Form Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseReceiveForm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/screening:
    post:
      summary: Submits A Screening Form For A Patient
      tags:
        - Patients
      operationId: submitScreening
      requestBody:
        description: This is used to submit a new Screening Form for an already existing patient
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScreeningForm"
      responses:
        "201":
          description: Patient Screening Form Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/screening/{idParam}:
    get:
      summary: Fetches A Patient's Screening Form Details
      tags:
        - Patients
      operationId: getScreening
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient Screening Form Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScreeningForm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/self-pay:
    post:
      summary: Submits A Self Pay Form For A Patient
      tags:
        - Patients
      operationId: submitSelfPay
      requestBody:
        description: This is used to submit a new Self Pay Form for an already existing patient
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SelfPayForm"
      responses:
        "201":
          description: Patient SelfPay Form Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/self-pay/{idParam}:
    get:
      summary: Fetches A Patient's SelfPay Form Details
      tags:
        - Patients
      operationId: getSelfPay
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient SelfPay Form Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SelfPayForm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/termination-policy:
    post:
      summary: Submits A Termination Policy Form For A Patient
      tags:
        - Patients
      operationId: submitTerminationPolicy
      requestBody:
        description: This is used to submit a new TerminationPolicy Form for an already existing patient
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TerminationPolicyForm"
      responses:
        "201":
          description: Patient TerminationPolicy Form Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/termination-policy/{idParam}:
    get:
      summary: Fetches A Patient's TerminationPolicy Form Details
      tags:
        - Patients
      operationId: getTerminationPolicy
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient TerminationPolicy Form Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TerminationPolicyForm"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/treatment-consent:
    post:
      summary: Submits A TreatmentConsentTelehealthInPersonTreatmentConsent Form For A Patient
      tags:
        - Patients
      operationId: submitTreatmentConsentTelehealthInPersonTreatmentConsent
      requestBody:
        description: This is used to submit a new TreatmentConsentTelehealthInPersonTreatmentConsent Form for an already existing patient
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TreatmentConsentTelehealthInPersonTreatmentConsent"
      responses:
        "201":
          description: Patient TreatmentConsentTelehealthInPersonTreatmentConsent Form Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/patients/forms/treatment-consent/{idParam}:
    get:
      summary: Fetches A Patient's TreatmentConsentTelehealthInPersonTreatmentConsent Form Details
      tags:
        - Patients
      operationId: getTreatmentConsentTelehealthInPersonTreatmentConsent
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Patient TreatmentConsentTelehealthInPersonTreatmentConsent Form Details Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TreatmentConsentTelehealthInPersonTreatmentConsent"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  #  /api/v1/patients/forms:
  #    post:
  #      summary: Handles All Form Submissions
  #      tags:
  #        - Forms
  #      operationId: submitForms
  #      requestBody:
  #        description: Submit All Kinds of Form
  #        required: true
  #        content:
  #          application/json:
  #            schema:
  #              $ref: "#/components/schemas/Forms"
  #      responses:
  #        "201":
  #          description: Form Submitted Successfully
  #          content:
  #            application/json:
  #              schema:
  #                $ref: "#/components/schemas/Success"
  #        "401":
  #          $ref: "#/components/responses/Unauthorized"
  #        "403":
  #          $ref: "#/components/responses/Forbidden"
  #        "400":
  #          $ref: "#/components/responses/BadRequest"
  #        "422":
  #          $ref: "#/components/responses/UnProcessableEntity"
  #        "500":
  #          $ref: "#/components/responses/InternalServerError"

  /api/v1/appointments:
    post:
      summary: Submits An Appointment
      tags:
        - Appointment
      operationId: submitAppointment
      requestBody:
        description: Submit All Kinds of Form
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Appointment"
      responses:
        "201":
          description: Appointment Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/appointments/search:
    post:
      summary:
        Searches and returns all Appointments matching all of the search criteria fields passed.
        The number of search results shall be constrained by the limit params in Query.
        Search criteria, applies an **AND** based search on all fields passed in the request.
        If the search criteria is empty/null, it returns list of appointments.
      tags:
        - Appointment
      operationId: listAppointments
      parameters:
        - description: The page number (starting from 1)
          $ref: "#/components/parameters/PageParam"
        - description: How many items to return at one time (max 100)
          $ref: "#/components/parameters/limitParam"
      requestBody:
        description:
          "Search criteria, applies an **AND** based search on all fields\
                \ passed in the request."
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AppointmentSearch"
      responses:
        "200":
          description: Appointment Records Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedAppointments"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/appointments/{idParam}:
    get:
      summary: Fetches An Appointment By ID
      tags:
        - Appointment
      operationId: getAppointment
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Appointment Retrieved Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Appointment"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/appointments/update:
    put:
      summary: Updates An Appointment
      tags:
        - Appointment
      operationId: updateAppointment
      requestBody:
        description: Submit All Kinds of Form
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Appointment"
      responses:
        "201":
          description: Appointment Updated Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/reviews:
    post:
      summary: Submits A Review
      tags:
        - Review
      operationId: submitReview
      requestBody:
        description: Patients have the opportunity to share their personal experiences and testimonials with others, providing a platform for them to express their thoughts, feelings, and outcomes. This allows other patients to gain insight and perspective from those who have gone through similar experiences, fostering a sense of community and support.
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Review"
      responses:
        "201":
          description: Review Submitted Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/reviews/search:
    post:
      summary:
        Searches and returns all Reviews matching all of the search criteria fields passed.
        The number of search results shall be constrained by the limit params in Query.
        Search criteria, applies an **AND** based search on all fields passed in the request.
        If the search criteria is empty/null, it returns list of reviews.
      tags:
        - Review
      operationId: listReviews
      parameters:
        - description: The page number (starting from 1)
          $ref: "#/components/parameters/PageParam"
        - description: How many items to return at one time (max 100)
          $ref: "#/components/parameters/limitParam"
      requestBody:
        description:
          "Search criteria, applies an **AND** based search on all fields\
                \ passed in the request."
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewSearch"
      responses:
        "200":
          description: Review Records Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedReviews"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/reviews/{idParam}:
    get:
      summary: Fetches A Review Record Using A Uniques idParam
      tags:
        - Review
      operationId: getReview
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Review Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Review"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/reviews/publish/{idParam}:
    get:
      summary: Fetches A Review Record Using A Unique idParam And Publishes it
      tags:
        - Review
      operationId: publish
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Review Published Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/reviews/unpublish/{idParam}:
    get:
      summary: Fetches A Review Record Using A Unique idParam And Un-publishes it, thereby Sending It To The List Of Drafts
      tags:
        - Review
      operationId: unpublish
      parameters:
        - $ref: "#/components/parameters/idParam"
      responses:
        "200":
          description: Review Unpublished Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Success"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/reviews/published:
    get:
      summary: Fetches All Published Reviews
      tags:
        - Review
      operationId: getPublished
      parameters:
        - description: The page number (starting from 1)
          $ref: "#/components/parameters/PageParam"
        - description: How many items to return at one time (max 100)
          $ref: "#/components/parameters/limitParam"
      responses:
        "200":
          description: Published Reviews Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedReviews"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/reviews/drafts:
    get:
      summary: Fetches All Draft Reviews
      tags:
        - Review
      operationId: getDrafts
      parameters:
        - description: The page number (starting from 1)
          $ref: "#/components/parameters/PageParam"
        - description: How many items to return at one time (max 100)
          $ref: "#/components/parameters/limitParam"
      responses:
        "200":
          description: Draft Reviews Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedReviews"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/auth/login:
    post:
      summary:
        Logs in a user into the system
      tags:
        - Authentication
      operationId: login
      requestBody:
        description:
          "Takes a email and password of which are required"
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SignInRequest"
      responses:
        "200":
          description: Review Records Fetched Successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JwtAuthenticationResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnProcessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

